package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// High-Contrast Trustworthy Government & Economic Empowerment Palette
// Official Bharat Service Identity: Deep Navy, Saffron, and Growth Emerald

// Light Theme (Bright White & High Contrast)
val GovBlueDark = Color(0xFF041E42)           // Deepest authoritative navy for headings and high contrast
val GovBluePrimary = Color(0xFF0A3871)        // Standard Indian Government Portal Blue
val GovBlueLight = Color(0xFFEFF6FC)          // Soft crisp blue tint for card sections
val GovBlueContainer = Color(0xFFDBEAFE)      // Subtle badge container
val GovBlueAccent = Color(0xFF1D4ED8)         // Vibrant royal blue accent

val GrowthGreen = Color(0xFF15803D)           // High contrast emerald green (financial prosperity)
val GrowthGreenLight = Color(0xFFDCFCE7)      // Soft green pill badge
val GrowthGreenDark = Color(0xFF14532D)       // Dark green for text on light green

val SaffronAccent = Color(0xFFD97706)         // Tiranga Saffron / Amber accent
val SaffronLight = Color(0xFFFEF3C7)          // Saffron badge background
val SaffronDark = Color(0xFF92400E)           // Dark saffron text

val SlateDark = Color(0xFF0F172A)             // Deep slate for primary body text (14:1 contrast ratio)
val SlateMedium = Color(0xFF334155)           // Crisp slate for secondary text (8:1 contrast ratio)
val SlateLight = Color(0xFF475569)            // Legible supporting text (5:1 contrast ratio)
val SlateBorder = Color(0xFFE2E8F0)           // Crisp card and chip border
val SlateBorderStrong = Color(0xFFCBD5E1)     // High-definition separator line

val BackgroundLight = Color(0xFFF8FAFC)       // Crisp clean off-white canvas
val SurfaceWhite = Color(0xFFFFFFFF)          // Pure radiant white for cards, sheets, topbars

// Dark Theme (Polished High-Contrast Night Mode)
val GovBluePrimaryDark = Color(0xFF93C5FD)    // Accessible light blue on dark
val GrowthGreenDarkTheme = Color(0xFF86EFAC)  // Accessible light green on dark
val SaffronAccentDark = Color(0xFFFBBF24)     // Accessible amber on dark
val BackgroundDark = Color(0xFF090E17)        // Deep slate night canvas
val SurfaceDark = Color(0xFF111827)           // Elevated surface
val SurfaceDarkVariant = Color(0xFF1F2937)    // Secondary elevated surface
val SlateBorderDark = Color(0xFF374151)       // Dark card outline
